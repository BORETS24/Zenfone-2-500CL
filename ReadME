Clone this source to your local machine(if you download as zip, you broke simbolic links)

For build kernel:

make -j4 -f KernelMakefile TARGET_DEVICE=redhookbay modules_install

For configuring kernel open terminal in out/target/product/redhookbay/linux/kernel and input

make menuconfig

